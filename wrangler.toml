name = "email-cf-svc"
compatibility_date = "2024-01-01"

[observability]
enabled = true

# Email binding for sending emails (if using unsafe.bindings)
# Note: This requires the sender to be verified in Cloudflare dashboard
[[unsafe.bindings]]
name = "EMAIL"
type = "email"